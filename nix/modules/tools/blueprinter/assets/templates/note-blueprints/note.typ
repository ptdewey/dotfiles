// #set page(columns: 2, margin: 1.8cm)
// #set columns(gutter: 5% + 5pt)

#align(
  center,
  text(17pt)[
    *#upper([{{ .Dir }}]) - {{ .ParentDir }}* \
    {{ .Date }}
  ],
)

#set heading(numbering: "1.")

= Heading

